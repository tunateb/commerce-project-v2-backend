{
  "kind": "collectionType",
  "collectionName": "purchase",
  "info": {
    "name": "purchase"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "status": {
      "type": "enumeration",
      "enum": [
        "delivered",
        "ordered"
      ]
    },
    "user": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "purchases"
    }
  }
}
